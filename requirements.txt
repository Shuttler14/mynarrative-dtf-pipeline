# DTF Automation Dependencies
# Optimized for Vercel serverless (500MB limit)

# Core
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Image Processing (lightweight)
Pillow>=10.2.0
numpy>=1.26.0

# AI / ML
openai>=1.12.0

# Cloud Storage
boto3>=1.34.0

# PDF Generation
reportlab>=4.0.9

# Async
httpx>=0.26.0
aiofiles>=23.2.1

# Heavy dependencies (optional - uncomment for full features)
# rembg>=2.0.50  # Background removal (adds ~200MB with ML models)
# opencv-python-headless>=4.9.0.80  # Not used in current code